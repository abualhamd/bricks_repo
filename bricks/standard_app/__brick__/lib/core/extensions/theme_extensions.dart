import 'package:flutter/material.dart';

import '../theming.dart';

extension ThemeExtensions on ThemeData {
  // AppPadding get appPadding => extension<AppPadding>()!;
  // AppColors get appColors => extension<AppColors>()!;
  // AppBorderRadius get appBorderRadius => extension<AppBorderRadius>()!;
  // AppSpacing get appSpacing => extension<AppSpacing>()!;
  // AppIcons get appIcons => extension<AppIcons>()!;
  // AppSizes get appSizes => extension<AppSizes>()!;
}
